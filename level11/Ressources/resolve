
Faille deja vu.

A la racine on voit un fichier lua qui est deja lance.

Pour se connecter il faut utiliser le port 5151. Donc on va utiliser telnet pour aller dessus.

On peut voir aussi qu'il utilise le echo du terminal pour pourvoir obtenir le sha1sum.

Donc:

telnet 127.0.0.1 5151

Il demande le mot de passe donc on test ca: ; getflag > /tmp/c
