In the /home, we see :
	- a lua script

In order to connect, we use telnet with the port 5151.

The script uses the shell "echo" in order to get the shasum.

So
telnet 127.0.0.1 5151

And when it asks for the password, we answer: ; getflag > /tmp/c
